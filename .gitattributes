# The archive is rewritten by CI on every run; normalise it so line endings never churn.
data/archive/** text eol=lf

# Generated data and vendored model code should not skew the repository's language stats.
data/archive/** linguist-generated=true
eisbach/model/duet/** linguist-vendored=true
archive/** linguist-documentation=true
