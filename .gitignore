__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Scratch data. The archive under data/archive/ is the exception: it is the only copy
# of every forecast we have ever made, and of the DWD forecasts as they were issued.
data/*
!data/archive/
!data/archive/**

# The model checkpoint is fetched on demand and verified against a pinned SHA256,
# so it never belongs in git.
data/model/

# Generated outputs. These are published to the `outputs` branch by CI rather than
# committed to main — see .github/workflows/forecast.yml. Committing them here is what
# grew .git to 175 MB.
Prediction.png
Prediction_Backtest.png
Prediction.csv

*.log
*.pid

# Working notes, deliberately untracked.
TODO.md
